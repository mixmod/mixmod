IF(APPLE)
	FILE(GLOB files "*.dylib*")
	INSTALL(FILES ${files} DESTINATION LIB/thirdparty)
ELSEIF(WIN32)
	FILE(GLOB files "*.dll*")
	INSTALL(FILES ${files} DESTINATION BIN)
        INSTALL(FILES ${files} DESTINATION ${CMAKE_SOURCE_DIR}/BIN/${CMAKE_BUILD_TYPE})
ELSE()
	FILE(GLOB files "*.so*")
	INSTALL(FILES ${files} DESTINATION LIB/thirdparty)
ENDIF()
